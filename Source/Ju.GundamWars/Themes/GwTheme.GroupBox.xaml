<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options" 
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:core="clr-namespace:Ju.GundamWars;assembly=Ju.GundamWars.Core"
	>

    <!-- #region Generic -->

    <!-- #endregion -->

    <!-- #region Filter -->

	<!-- Common -->

    <!-- CoMobiles -->

    <!-- Cuspas -->

    <!-- Mobiles -->

    <!-- Pilots -->

    <!-- Supports -->

    <!-- Tags -->

    <!-- #endregion -->

    <!-- #region Entry -->

    <Style x:Key="GwEntryGroupBox" TargetType="{x:Type GroupBox}" BasedOn="{StaticResource MaterialDesignCardGroupBox}">
        <Setter Property="Margin" Value="0 0 0 16" />
        <!--<Setter Property="Padding" Value="8 4" />-->
        <Setter Property="Width" Value="480" />
        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp0" />
        <Setter Property="materialDesign:ColorZoneAssist.Mode" Value="Custom" />
        <Setter Property="materialDesign:ColorZoneAssist.Foreground" Value="{DynamicResource MaterialDesignBody}" />
        <!--<Setter Property="materialDesign:ColorZoneAssist.Background" Value="Transparent" />-->
        <!--<Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid Height="24">
                            <TextBlock Text="{Binding}" />
                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>-->
    </Style>


    <!-- Common -->

    <!-- CoMobiles -->

    <!-- Cuspas -->

    <!-- Mobiles -->

    <!-- Pilots -->

    <!-- Supports -->

    <!-- Tags -->

    <!-- #endregion -->

</ResourceDictionary>
