<UserControl x:Class="Ju.GundamWars.Controls.AddMenuUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mahApps="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:const="clr-namespace:Ju.GundamWars.Const;assembly=Ju.GundamWars.Core"
    xmlns:root="clr-namespace:Ju.GundamWars"
    xmlns:main="clr-namespace:Ju.GundamWars.Systems"
    xmlns:local="clr-namespace:Ju.GundamWars.Controls"
    mc:Ignorable="d"
    SnapsToDevicePixels="True"
    >
    <Menu>
        <MenuItem Height="46">
            <MenuItem.Header>
                <StackPanel Height="14" Orientation="Horizontal">
                    <materialDesign:PackIcon VerticalAlignment="Center" Margin="0 0 8 0" Kind="PlusBoxOutline" />
                    <TextBlock VerticalAlignment="Center" Text="New..." FontFamily="Yu Gothic UI" FontSize="13" FontWeight="SemiBold" />
                </StackPanel>
            </MenuItem.Header>
            <MenuItem Header="For MS" Height="48" Command="{Binding OpenEntryAsNewForMsCommand}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="{Binding Source={x:Static const:GwIcon.MobileSuit}}" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="For MA" Height="48" Command="{Binding OpenEntryAsNewForMaCommand}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="{Binding Source={x:Static const:GwIcon.MobileArmor}}" />
                </MenuItem.Icon>
            </MenuItem>
        </MenuItem>
    </Menu>
</UserControl>
