<UserControl x:Class="Ju.GundamWars.Commons.View.MessageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mahApps="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:commons="clr-namespace:Ju.GundamWars.Commons.View"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance commons:MessageViewModel}"
    d:DesignHeight="400"
    d:DesignWidth="800"
    >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Margin="16" Text="{Binding Message}" />
        <StackPanel Grid.Row="1" Margin="16" Orientation="Horizontal">
            <Button Style="{StaticResource GwDialog.CancelButton}" Visibility="{Binding IsCancelVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Button Style="{StaticResource GwDialog.NoButton}" Visibility="{Binding IsNoVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Button Style="{StaticResource GwDialog.OkButton}" Visibility="{Binding IsOkVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Button Style="{StaticResource GwDialog.YesButton}" Visibility="{Binding IsYesVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </StackPanel>
    </Grid>
</UserControl>
