<UserControl x:Class="Ju.GundamWars.Commons.View.StatusBarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mahApps="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:core="clr-namespace:Ju.GundamWars;assembly=Ju.GundamWars.Core"
    xmlns:commons="clr-namespace:Ju.GundamWars.Commons.View"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance commons:BizListViewModelBase`1}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    >
    <StatusBar DockPanel.Dock="Bottom" Margin="2 0 0 2" Height="24" Padding="4 0" Background="Transparent">
        <StatusBarItem>
            <TextBlock Margin="0" VerticalAlignment="Center">
                <Run Text="{Binding ItemsView.Count, Mode=OneWay}"/>
                <Run Text="/" />
                <Run Text="{Binding Items.Count, Mode=OneWay}"/>
                <Run Text="items" />
            </TextBlock>
        </StatusBarItem>
        <StatusBarItem Margin="8 0 0 0" Visibility="{Binding IsCountableChecked, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Margin="0" VerticalAlignment="Center">
                <Run Text="{Binding FilteredCheckedCount, Mode=OneWay}"/>
                <Run Text="/" />
                <Run Text="{Binding CheckedCount, Mode=OneWay}"/>
                <Run Text="checked" />
            </TextBlock>
        </StatusBarItem>
    </StatusBar>
</UserControl>
