<UserControl x:Class="Ju.GundamWars.Tags.TagListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrl="clr-namespace:Ju.GundamWars.Controls"
    xmlns:local="clr-namespace:Ju.GundamWars.Tags"
    xmlns:const="clr-namespace:Ju.GundamWars.Const;assembly=Ju.GundamWars.Core"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance local:TagListViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    SnapsToDevicePixels="True"
    >
    <UserControl.Resources>
        <Style TargetType="{x:Type ItemsControl}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ItemsControl}">
                        <ScrollViewer>
                            <ItemsPresenter />
                        </ScrollViewer>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBox Style="{StaticResource GwEntryTextBox}" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="16">
            <materialDesign:PackIcon DockPanel.Dock="Left" Margin="0 0 8 0" VerticalAlignment="Center" Width="24" Height="24" Kind="Tags" />
            <TextBlock Style="{StaticResource MaterialDesignHeadline6TextBlock}" VerticalAlignment="Center" FontWeight="Regular">
                <Run Text="Tag" />
                <Run Text="List" />
            </TextBlock>
        </DockPanel>
        <Grid Grid.Row="1" Margin="16 0 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <GroupBox Grid.Column="0" Header="MS / MA">
                <ItemsControl ItemsSource="{Binding MobileTags}" Focusable="False" />
            </GroupBox>
            <GroupBox Grid.Column="1" Header="Pilot">
                <ItemsControl ItemsSource="{Binding PilotTags}" Focusable="False" />
            </GroupBox>
            <GroupBox Grid.Column="2" Header="Support">
                <ItemsControl ItemsSource="{Binding SupportTags}" Focusable="False" />
            </GroupBox>
            <GroupBox Grid.Column="3" Header="Co-Unit">
                <ItemsControl ItemsSource="{Binding CoMobileTags}" Focusable="False" />
            </GroupBox>
            <GroupBox Grid.Column="4" Header="Cuspa">
                <ItemsControl ItemsSource="{Binding CuspaTags}" Focusable="False" />
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
