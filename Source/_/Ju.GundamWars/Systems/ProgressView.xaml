<UserControl
    x:Class="Ju.GundamWars.Systems.ProgressView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Ju.GundamWars.Systems"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance local:ProgressViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    >
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
            <materialDesign:PackIcon VerticalAlignment="Center" Kind="{Binding Icon}" Width="48" Height="48" />
            <TextBlock Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="16 0 0 2" VerticalAlignment="Bottom" Text="{Binding Title}" />
        </StackPanel>
        <ProgressBar Margin="0 16 0 0" HorizontalAlignment="Center" Width="280" Height="16" Maximum="{Binding Maximum}" Value="{Binding Value}" materialDesign:TransitionAssist.DisableTransitions="True" />
        <TextBlock Margin="2 2 2 0" HorizontalAlignment="Left" Text="{Binding Message}" />
    </StackPanel>
</UserControl>
